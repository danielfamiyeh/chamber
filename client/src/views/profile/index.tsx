import React from 'react';
import { View } from 'react-native';

import styles from './styles';

const ProfileView = ({ navigation: { goBack, navigate } }) => {
  return <View style={styles.container}>{/**/}</View>;
};

export default ProfileView;
