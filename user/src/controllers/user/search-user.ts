import { Response } from 'express';
import { models } from '@danielfamiyeh/chamber-common/dist/models';

import { SearchUserRequest } from '../../../types';

export const searchUser = (req: SearchUserRequest, res: Response) => {};
